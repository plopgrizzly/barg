[package]
name = "barg"
version = "0.1.0"
authors = ["Jeron Lau <jeron.lau@plopgrizzly.com>"]

[dependencies]
# For Color Translations & sRGBA VFrame.
afi = { path = "../afi" }
# For Text Rendering
fonterator = { path = "../fonterator" }
# For Vector Math
ami = "0.13"
# Renderer
footile = {path = "../footile"} # "0.1"
arrayvec = { version = "0.4", default-features = false }
ordered-float = "0.5"
approx = { version = "0.3", default-features = false }

[dev-dependencies]
png = "0.12"
